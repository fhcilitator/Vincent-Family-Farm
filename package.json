{
  "name": "vibe-agent",
  "private": true,
  "version": "0.1.0",
  "description": "Android app and dev-box companion agent for driving Claude Code from a phone",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspace=@vff/protocol && npm run build --workspace=@vff/client-core && npm run build --workspace=@vff/agent",
    "build:client": "npm run build --workspace=@vff/protocol && npm run build --workspace=@vff/client-core",
    "eas-build-post-install": "npm run build:client",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "agent": "npm run start --workspace=@vff/agent",
    "harness": "npm run harness --workspace=@vff/agent"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
